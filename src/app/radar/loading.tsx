export default function RadarLoading() {
  return <main aria-busy="true" aria-live="polite"><p className="eyebrow">RADAR · LOADING</p><h1>Loading signal radar</h1><p>Retrieving the selected run. No figures are invented while the run is loading.</p></main>;
}
